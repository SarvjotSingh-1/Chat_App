// import { Resend } from "resend";
// // import dotenv from "dotenv/config";

// import dotenv from "dotenv";
// dotenv.config({ quiet: true });

// export const resendClient = new Resend.Client(process.env.RESEND_API_KEY);

// export const sender = {
//   email: process.env.RESEND_SENDER_EMAIL,
//   name: process.env.RESEND_SENDER_NAME,
// };

// import { Resend } from "resend";
// import dotenv from "dotenv";

// dotenv.config({ quiet: true });

// export const resendClient = new Resend(process.env.RESEND_API_KEY);

// export const sender = {
//   email: process.env.RESEND_SENDER_EMAIL,
//   name: process.env.RESEND_SENDER_NAME,
// };
